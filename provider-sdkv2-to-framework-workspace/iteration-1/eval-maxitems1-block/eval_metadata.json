{
  "eval_id": 4,
  "eval_name": "eval-maxitems1-block",
  "prompt": "Migrate just the schema (not the CRUD methods, not the tests) of the resource defined in <openstack-clone>/openstack/resource_openstack_lb_pool_v2.go from terraform-plugin-sdk/v2 to terraform-plugin-framework. Pay particular attention to how MaxItems: 1 blocks should be represented in the framework \u2014 make the right call between SingleNestedAttribute and ListNestedBlock and explain your reasoning.",
  "expectations": [
    "MaxItems: 1 blocks have been migrated either to SingleNestedAttribute or to ListNestedBlock with listvalidator.SizeAtMost(1)",
    "The choice between SingleNestedAttribute and ListNestedBlock is justified in either an inline comment, a commit message, or the response text",
    "If SingleNestedAttribute was chosen, the response notes that this is a syntactic HCL change for practitioners (block syntax \u2192 attribute syntax)",
    "go build ./... passes against <clone-path>",
    "User-facing schema attribute names are unchanged"
  ]
}