# Summary

The skill's bundled `audit_sdkv2.sh` script was run against the `terraform-provider-openstack` repository (module `github.com/terraform-provider-openstack/terraform-provider-openstack/v3`, SDKv2 version v2.38.1) and produced a full inventory of 236 non-test Go source files covering 105 resources and 64 data sources. Key findings: 822 `ForceNew` fields, 141 `ValidateFunc`/`ValidateDiagFunc` occurrences, 101 custom `Importer` blocks, 68 `Timeouts` blocks, 16 `MaxItems:1` candidates (block-vs-nested decisions), 6 `CustomizeDiff` hooks, 5 `StateFunc` hooks, 10 `DiffSuppressFunc` hooks, and one `StateUpgraders`/`SchemaVersion` (on `objectstorage_container_v1`). The migration checklist organises all 169 components (resources + data sources) into five phases — provider skeleton first, then eight high-complexity resources (carrying `CustomizeDiff`, `StateFunc`, `DiffSuppressFunc`, or state upgraders), followed by medium-complexity resources (nested blocks + `MaxItems:1`), standard resources, and finally all data sources — with per-entry TDD-gated subtasks aligned to the skill's 12-step single-release-cycle workflow.
