The `terraform-provider-openstack` (SDKv2 v2.38.1) contains 236 non-test Go source files, 109 resources, and 64 data sources (173 total migrateable units), with a single `StateUpgrader` on `openstack_objectstorage_container_v1` (the highest-risk file and the recommended first migration target), 637 `ForceNew` occurrences to convert to `RequiresReplace` plan modifiers, 140 validators, 68 Timeouts blocks, 101 custom importers (most composite-ID), 11 `MaxItems:1` nested blocks requiring backward-compatible `ListNestedBlock` treatment, 3 `CustomizeDiff` functions to port to `ModifyPlan`, 4 `StateFunc` usages to replace with custom types, 8 `DiffSuppressFunc` usages needing intent analysis, and 113 files flagged for manual review before any edits begin; the recommended migration order is `objectstorage_container_v1` (StateUpgrader) → `compute_instance_v2` (complexity score 62) → `db_instance_v1` (45) → `networking_port_v2` (37), then service-area batches, with protocol v6 chosen as the default per HashiCorp's single-release-cycle guidance.
